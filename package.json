{
  "name": "the-toll-booth",
  "version": "1.0.0",
  "description": "Pay with productivity. Complete a micro-task before accessing distracting sites.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "preview": "echo 'Load the dist/ folder as an unpacked extension in Chrome'"
  },
  "dependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.26",
    "vite": "^5.4.0"
  }
}
